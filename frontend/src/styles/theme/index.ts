import { colors } from '../colors';

export const theme = {
    colors:{
        primary: colors.PRYMARY,
        secondary: colors.SECONDARY,
        tertiary: colors.TERTIARY,
        background: colors.BACKGROUND,
        backgroundLight: colors.BACKGROUND2,
        backgroundGray: colors.BACKGROUND3,
        red: colors.RED,
        green: colors.GREEN
    }
}