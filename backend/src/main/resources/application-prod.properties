spring.datasource.url=${DATABASE_URL}

default.sender=${EMAIL_SENDER:dynamousads@gmail.com}
default.recipient=${EMAIL_RECIPIENT:dynamousads@gmail.com}
spring.mail.host=smtp.gmail.com
spring.mail.username=${EMAIL_USERNAME:dynamousads@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:oevkxgcdgvvlektu}
spring.mail.smtp.port= 465
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.socketFactory.port = 465
spring.mail.properties.mail.smtp.socketFactory.class = javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback = false
spring.mail.properties.mail.smtp.starttls.enable = true
spring.mail.properties.mail.smtp.ssl.enable = true

spring.jpa.hibernate.ddl-auto=none

# Vercel Configuration
vercel.token=${VERCEL_TOKEN:vazio}
vercel.project.id=${VERCEL_PROJECT_ID:vazio}
vercel.team.id=${VERCEL_TEAM_ID:}

# Stripe Configuration
stripe.secret.key=${STRIPE_SECRET_KEY:vazio}
stripe.success.url=${STRIPE_SUCCESS_URL:vazio}
stripe.cancel.url=${STRIPE_CANCEL_URL:vazio}
